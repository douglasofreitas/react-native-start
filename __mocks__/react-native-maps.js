/* eslint-disable react/prop-types */
import React from 'react';
import { View } from 'react-native';

export const Circle = ({ children }) => <View>{children}</View>;

export default ({ children }) => <View>{children}</View>;
