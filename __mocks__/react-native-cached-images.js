/* eslint-disable react/prop-types */
import React from 'react';
import { View } from 'react-native';

export const CachedImage = ({ children }) => <View>{children}</View>;

export default ({ children }) => <View>{children}</View>;
